export type AccessControlListResponseType = {
  name: string;
  id: string | number;
  staffCode: string | number;
  status: boolean;
  unit: string;
  showPassword?: boolean;
  newPasswrod?: string;
};
